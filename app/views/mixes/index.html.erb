<h1>Listing mixes</h1>

<table>
  <tr>
		<th>Artist</th>
    <th>Title</th>
  </tr>

<% @mixes.each do |mix| %>
  <tr>
		<td><%= h mix.artist.name %></td>
    <td><%= link_to h(mix.name), mix %></td>
  </tr>
<% end %>
</table>
